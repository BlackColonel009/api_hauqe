export function escapeHtml(value){return String(value??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#039;")}
export function hasPermission(user,code){return Array.isArray(user?.permissions)&&user.permissions.includes(code)}
export function refreshIcons(){if(window.lucide)window.lucide.createIcons({attrs:{"stroke-width":1.8}})}
export function formatDate(value){if(!value)return"—";const d=new Date(`${value}T00:00:00`);return Number.isNaN(d.getTime())?String(value):new Intl.DateTimeFormat("fr-FR",{day:"2-digit",month:"short",year:"numeric"}).format(d)}
export function showState(selector,message,{error=false}={}){const n=document.querySelector(selector);if(!n)return;n.hidden=false;n.className=`dashboard-api-state ${error?"error":""}`.trim();n.innerHTML=`<i data-lucide="${error?"triangle-alert":"info"}"></i><div><strong>${error?"Opération impossible":"Information"}</strong><span>${escapeHtml(message)}</span></div>`;refreshIcons()}
export function hideState(selector){const n=document.querySelector(selector);if(n)n.hidden=true}
export async function loadActiveModel(apiGet,objectType){try{const model=await apiGet(`/api/v1/scoring/models/active?objet_evalue=${encodeURIComponent(objectType)}`);const weights=await apiGet(`/api/v1/scoring/models/${model.id}/weights`);return{model,weights:(weights||[]).filter(x=>!x.statut||String(x.statut).toUpperCase()==="ACTIF")}}catch(error){if([403,404,409].includes(error?.status))return{model:null,weights:[],error};throw error}}
export function calculationMode(model){return String(model?.regle_calcul?.calculation_mode||"").trim().toUpperCase()}
export function renderModelBanner(container,bundle,label){const{model,weights,error}=bundle;if(!model){container.className="scoring-model-banner unavailable";container.innerHTML=`<span><i data-lucide="shield-alert"></i></span><div><strong>Aucun modèle publié actif — ${escapeHtml(label)}</strong><small>${error?.status===403?"Le compte courant ne possède pas SCORING.LIRE.":"Le calcul reste bloqué tant qu’un modèle approuvé n’est pas publié."}</small></div>`;refreshIcons();return}container.className="scoring-model-banner";container.innerHTML=`<span><i data-lucide="calculator"></i></span><div><strong>${escapeHtml(model.libelle||model.code||label)}</strong><small>${escapeHtml(model.code||"—")} · version ${escapeHtml(model.version||"—")} · ${escapeHtml(calculationMode(model)||"Mode non renseigné")} · ${escapeHtml(weights.length)} pondération(s) · approbation ${escapeHtml(model.reference_approbation||"—")}</small></div><span class="scoring-model-badge"><i data-lucide="badge-check"></i>Publié</span>`;refreshIcons()}
export function buildScoreInputs(container,bundle,prefix){const{model,weights}=bundle;if(!model){container.innerHTML='<div class="score-input-empty">Aucun modèle publié actif accessible.</div>';return}const mode=calculationMode(model);if(mode==="DIRECT_SCORE"){const min=model.regle_calcul?.score_min??"",max=model.regle_calcul?.score_max??"";container.innerHTML=`<label class="score-input-field"><span>Score direct <b>*</b></span><input id="${prefix}DirectScore" type="number" step="0.01" ${min!==""?`min="${escapeHtml(min)}"`:""} ${max!==""?`max="${escapeHtml(max)}"`:""} required><small>Valeur fournie selon la règle publiée.</small></label>`;return}if(!weights.length){container.innerHTML='<div class="score-input-empty">Le modèle actif ne possède aucune pondération active.</div>';return}container.innerHTML=`<div class="score-domain-grid">${weights.map(w=>{const max=mode==="SUM_DOMAIN_POINTS"?Number(w.valeur??0):100;return`<label class="score-input-field"><span>${escapeHtml(w.domaine||"Domaine")} <b>*</b></span><input type="number" min="0" max="${escapeHtml(max)}" step="0.01" data-score-domain="${escapeHtml(w.domaine||"")}" required><small>${mode==="SUM_DOMAIN_POINTS"?`Maximum ${escapeHtml(w.valeur??0)} point(s)`:`Pondération ${escapeHtml(w.valeur??0)}`}</small></label>`}).join("")}</div>`}
export function scorePayload(container,bundle,prefix){const mode=calculationMode(bundle.model),payload={modele_scoring_id:bundle.model?.id||null,score_direct:null,scores_domaines:{},sources:{}};if(mode==="DIRECT_SCORE"){const n=container.querySelector(`#${CSS.escape(prefix)}DirectScore`);if(!n?.value)throw new Error("Le score direct est obligatoire.");payload.score_direct=Number(n.value);return payload}for(const n of container.querySelectorAll("[data-score-domain]")){if(n.value==="")throw new Error(`La valeur du domaine ${n.dataset.scoreDomain} est obligatoire.`);payload.scores_domaines[n.dataset.scoreDomain]=Number(n.value)}return payload}
export async function runAction(task,options={}){return window.HAUQE_ACTION_LOADER?window.HAUQE_ACTION_LOADER.run(task,options):task()}
