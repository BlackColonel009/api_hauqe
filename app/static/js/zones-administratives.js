(async function(){
  'use strict';
  const api=await import('/static/js/core/api.js');
  const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
  let user=null,zones=[],editing=null,statusTarget=null,timer=null;
  const f={search:'',type:'',status:''};
  const esc=v=>String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');
  const icons=()=>window.lucide?.createIcons({attrs:{'stroke-width':1.8}});
  const has=p=>user?.permissions?.includes(p);
  function state(message,error=false){const n=$('#zonesApiState');n.hidden=false;n.className=`dashboard-api-state ${error?'error':''}`.trim();n.innerHTML=`<i data-lucide="${error?'triangle-alert':'info'}"></i><div><strong>${error?'Opération impossible':'Information'}</strong><span>${esc(message)}</span></div>`;icons()}
  async function run(task,options={}){return window.HAUQE_ACTION_LOADER?window.HAUQE_ACTION_LOADER.run(task,options):task()}
  function pathLabel(z){return z.path||[z.parent_name,z.nom].filter(Boolean).join(' › ')||z.nom||'—'}
  function filtered(){const q=f.search.toLowerCase();return zones.filter(z=>(!f.type||z.type_zone===f.type)&&(!f.status||z.statut===f.status)&&(!q||[z.nom,z.code,z.path,z.parent_name].filter(Boolean).join(' ').toLowerCase().includes(q)))}
  function summary(){const counts=t=>zones.filter(z=>z.type_zone===t&&z.statut!=='INACTIF').length;$('#zonesSummary').innerHTML=[['map','Zones',zones.length],['landmark','Régions',counts('REGION')],['map-pinned','Préfectures',counts('PREFECTURE')],['building-2','Communes',counts('COMMUNE')],['locate-fixed','Localités',counts('LOCALITE')]].map(([i,l,v])=>`<article><span><i data-lucide="${i}"></i></span><div><small>${l}</small><strong>${v}</strong></div></article>`).join('');icons()}
  function rows(){const data=filtered();$('#zonesEmpty').hidden=!!data.length;$('#zoneRows').innerHTML=data.map(z=>`<tr><td><div class="zone-name"><strong>${esc(z.nom)}</strong><small>${esc(pathLabel(z))}</small></div></td><td><span class="zone-type">${esc(z.type_zone||'—')}</span></td><td><code>${esc(z.code||'—')}</code></td><td>${esc(z.parent_name||'Racine')}</td><td>${z.latitude!=null&&z.longitude!=null?`${esc(z.latitude)}, ${esc(z.longitude)}`:'—'}</td><td><span class="user-status ${String(z.statut||'ACTIF').toLowerCase()}"><i></i>${esc(z.statut||'ACTIF')}</span></td><td><div class="zone-actions">${has('REFERENTIELS.MODIFIER')?`<button type="button" data-edit="${z.id}" title="Modifier"><i data-lucide="pencil"></i></button>`:''}${has('REFERENTIELS.DESACTIVER')?`<button type="button" data-status="${z.id}" title="Changer le statut"><i data-lucide="power"></i></button>`:''}</div></td></tr>`).join('');$$('[data-edit]').forEach(b=>b.onclick=()=>openForm(zones.find(z=>z.id===b.dataset.edit)));$$('[data-status]').forEach(b=>b.onclick=()=>openStatus(zones.find(z=>z.id===b.dataset.status)));icons()}
  function parentOptions(current=null){$('#zoneFormParent').innerHTML='<option value="">Aucune — niveau racine</option>'+zones.filter(z=>z.id!==current?.id&&z.statut!=='INACTIF').map(z=>`<option value="${z.id}">${esc(pathLabel(z))} · ${esc(z.type_zone)}</option>`).join('')}
  function openForm(z=null){editing=z;parentOptions(z);$('#zoneDialogTitle').textContent=z?'Modifier la zone':'Nouvelle zone';$('#zoneFormType').value=z?.type_zone||'';$('#zoneFormCode').value=z?.code||'';$('#zoneFormName').value=z?.nom||'';$('#zoneFormParent').value=z?.parent_id||'';$('#zoneFormLatitude').value=z?.latitude??'';$('#zoneFormLongitude').value=z?.longitude??'';$('#zoneFormStatus').value=z?.statut||'ACTIF';$('#zoneDialog').showModal();icons()}
  function payload(){const num=id=>{const v=$(id).value.trim();return v===''?null:Number(v)};return{type_zone:$('#zoneFormType').value,code:$('#zoneFormCode').value.trim()||null,nom:$('#zoneFormName').value.trim(),parent_id:$('#zoneFormParent').value||null,latitude:num('#zoneFormLatitude'),longitude:num('#zoneFormLongitude'),statut:$('#zoneFormStatus').value}}
  async function save(ev){ev.preventDefault();try{await run(()=>editing?api.apiPatch(`/api/v1/zones-administratives/${editing.id}`,payload()):api.apiPost('/api/v1/zones-administratives',payload()),{button:ev.submitter,title:'Zone administrative',message:editing?'Mise à jour de la zone':'Création de la zone',detail:'La hiérarchie et les doublons sont contrôlés côté serveur.'});$('#zoneDialog').close();await load();state(editing?'Zone mise à jour.':'Zone créée.')}catch(e){state(e?.message||'Enregistrement impossible.',true)}}
  function openStatus(z){statusTarget=z;$('#zoneStatusTitle').textContent=`Statut de ${z.nom}`;$('#zoneNewStatus').value=z.statut==='INACTIF'?'ACTIF':'INACTIF';$('#zoneStatusReason').value='';$('#zoneStatusDialog').showModal();icons()}
  async function saveStatus(ev){ev.preventDefault();if(!statusTarget)return;try{await run(()=>api.apiPatch(`/api/v1/zones-administratives/${statusTarget.id}/status`,{statut:$('#zoneNewStatus').value,motif:$('#zoneStatusReason').value.trim()||null}),{button:ev.submitter,title:'Zone administrative',message:'Changement du statut'});$('#zoneStatusDialog').close();await load();state('Statut de la zone mis à jour.')}catch(e){state(e?.message||'Changement impossible.',true)}}
  async function load(){zones=await api.apiGet('/api/v1/zones-administratives?include_inactive=true&limit=1000');summary();rows()}
  function bind(){document.addEventListener('submit',ev=>{if(ev.target.id==='zoneForm')save(ev);if(ev.target.id==='zoneStatusForm')saveStatus(ev)},true);$('#newZone').onclick=()=>openForm();$('#refreshZones').onclick=ev=>run(load,{button:ev.currentTarget,title:'Zones administratives',message:'Actualisation du référentiel'});$('#zoneSearch').oninput=e=>{clearTimeout(timer);timer=setTimeout(()=>{f.search=e.target.value.trim();rows()},180)};$('#zoneType').onchange=e=>{f.type=e.target.value;rows()};$('#zoneStatus').onchange=e=>{f.status=e.target.value;rows()};$('#resetZoneFilters').onclick=()=>{f.search=f.type=f.status='';$('#zoneSearch').value='';$('#zoneType').value='';$('#zoneStatus').value='';rows()}}
  try{bind();[user,zones]=await Promise.all([api.apiGet('/api/v1/me'),api.apiGet('/api/v1/zones-administratives?include_inactive=true&limit=1000')]);$('#newZone').hidden=!has('REFERENTIELS.CREER');summary();rows()}catch(e){state(e?.message||'Référentiel géographique indisponible.',true)}icons();
})();
